# My Markdown Website

This repository contains a minimal Jekyll website built entirely using Markdown. 

## Getting Started

1. Make sure you have Ruby and Bundler installed.
2. Run `bundle install` to install the dependencies.
3. Run `bundle exec jekyll serve` to serve the site locally.

Open your browser at http://localhost:4000 to view the website. 